# WARP+ Installer for Marzban Nodes

Install WARP+ on your Marzban nodes using the following command:

```bash
bash <(curl -s https://raw.githubusercontent.com/jellyenderson/warp_plus_installer/main/setup_warp.sh)
